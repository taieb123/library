



{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block pageTitle %}Afficher auteur{% endblock %}

{% block body %}

<div class="col-md-12">
    <!-- Advanced Tables -->
    <div class="panel panel-default">
        <div class="panel-heading">
                <div class="card-title">
                    <div class="title">Categories</div>
                </div>
        </div>
        <div class="panel-body">
             <table class="table">
                 <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ category.designation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
            </table>
        </div>
    </div>
</div>
    

<div class="col-md-12">
    <!-- Advanced Tables -->
    <div class="panel panel-default">
        <div class="panel-body">
            <div class="d-flex">
               <a href="{{ path('categories_index') }}" class="btn btn-default mr-20">back to list</a>

    <a href="{{ path('categories_edit', {'id': category.id}) }}" class="btn btn-warning mr-20">edit</a>

    {{ include('categories/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
