{% extends 'base.html.twig' %}

{% block title %}New Categories{% endblock %}

{% block body %}
    {{ include('categories/_form.html.twig') }}

<div class="col-md-12">
    <!-- Advanced Tables -->
    <div class="panel panel-default">
        <div class="panel-body">
            <div class="d-flex">
                <a href="{{ path('categories_index') }}" class="btn btn-default">back to list</a>
            </div>
        </div>
    </div>
</div>

{% endblock %}
